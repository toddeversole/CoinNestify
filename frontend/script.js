// Wait for the DOM to be fully loaded
document.addEventListener("DOMContentLoaded", function() {
    // Get a reference to the "Invest Now" button
    var investButton = document.getElementById("invest-button");

    // Add a click event listener to the button
    investButton.addEventListener("click", function() {
        // Replace this with your code to open the payment gateway pop-up/modal
        alert("Payment gateway pop-up/modal will open here!");
    });
});
